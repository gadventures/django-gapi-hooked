"""
Just a skeleton of a Django app so that we can test making requests to our view
under various configurations.

Because the behaviour of the view relies on Django's settings module, it's hard
to really test it without some of this structure.
"""
